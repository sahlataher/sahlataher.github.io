HW 1-

1. Describe the path an HTTP Request takes from a browser to your GitHub Pages site.
The browser requests the URL address using the GET method. If the HTTP request is successful, the browser uses the information from the response headers sent back from the server to render the HTML code to the screen.

2. We discussed Docker Containers in class. Explain how a Docker Container differs from the environment provided by GitHub Pages.
Docker packages applications into containers which includes everything needed for the application to run. GitHub Pages is used for hosting websites and displaying content.

4. AI Attribution: If you used GenAI (ChatGPT, Claude, etc.) to help write code, you must include the prompt you used and explain one logic error the AI made that you had to fix manually.
I did not use AI to write any code.

HW 2-

1. Explain the difference between flex-direction: row and flex-direction: column.
Flex-direction: row displays the elements in a horizontal format and flex-direction: column displays items vertically.

2. Why is it important to use relative units (like %, vh, or rem) instead of fixed pixels (px) for
responsive design?
Relative units are scalable and can be adjusted easily to different devices and screen sizes.

4. AI Attribution: If you used GenAI (ChatGPT, Claude, etc.) to help write code, you must include the prompt you used and explain one logic error the AI made that you had to fix manually.
I used ChaGPT to help me set up my links in the top navigation bar which weren't clickable to their destination before. I learned that I needed to by set up id classes for the elements in the nav bar and I manually set up those id classes and their names.
